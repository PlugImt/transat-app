import { Text } from "react-native";
import Page from "@/components/common/Page";

export const Caps = () => {
  return (
    <Page>
      <Text className="h1 m-4">Caps</Text>
    </Page>
  );
};

export default Caps;
